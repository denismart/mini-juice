/**
 * Проверяет соответствует ли индекс последнему элементу в массиве
 * @param {Array} array - Массив.
 * @param {number} num - Индекс.
 * @return {boolean}
 */
const isLast = (array, num) => array.length - 1 === num;

export default isLast;

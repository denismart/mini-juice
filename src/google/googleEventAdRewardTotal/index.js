import googleEventAd from '../googleEventAd';

/**
 * Посылает событие в гугл аналитику
 */
const googleEventAdRewardTotal = () => googleEventAd('reward-total');

export default googleEventAdRewardTotal;

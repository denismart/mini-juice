import googleEventAd from '../googleEventAd';

/**
 * Посылает событие в гугл аналитику
 */
const googleEventAdInterstitialTotal = () => googleEventAd('interstitial-total');

export default googleEventAdInterstitialTotal;
